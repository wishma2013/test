import java.nio.file.Files
import java.security.SecureRandom

import scala.collection.mutable.ListBuffer
import scala.runtime.BoxedUnit
/**
 * Created by eilir on 2019/2/25.
 */
object Main{
//  def main(args: Array[String]): Unit ={
//    Longlines.processFile(getClass.getResource("").getPath + "mytest", 0)
//    val bs = new Array[byte];
//    println(bs);
//    val t = new SecureRandom
//    val iv = new Array[Byte](16)
//    t.nextBytes(iv)

    /**
     * Created by eilir on 2019/12/21.
     */
//    val configPath = ActorEnv.getAbsolutePath("conf/server.conf")
//    println(configPath)
//    if (Files.exists(configPath)) {
//      println(configPath)
//      System.setProperty("config.file", configPath.toString)
//    }
//    println(sys.env)
//    println(sys.env.get("BINTRAY_USER"))
//
//  }
    /**
     * @MethodName: main
     * @Description: 虽然会报错，但是运行良好
     * @Param: [args]
     * @Return: {@link java.lang.Object}
     * @Author: eilir
     * @Date: 2019/12/24 上午10:47
     **/
//    def main(args: Array[String]): Unit = {
//        val tabby = Kitten(id = 4321, attributes = Seq("female", "tabby", "kid-friendly"))
//        val prefs = BuyerPreferences(List("male", "tabby", "kid-friendly"))
//        println(Logic.matchlikelihood(tabby, prefs))
//        return
//    }

}
